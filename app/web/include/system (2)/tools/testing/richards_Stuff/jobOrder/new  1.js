(screen.width>=1200)
screen.availWidth>=1200


if (screen.availWidth<=720) //if screen width is less than 720px wide
	getElementById("smForm") showDiv;
else if  (screen.availWidth >=721 && screen.availWidth<=1199) //if screen width is less than 1200 greater than 720px wide
	getElementById("medForm") showDiv;
else (screen.availWidth >=1200) //if screen width is greater than 1200px wide
	getElementById("lgForm") showDiv;